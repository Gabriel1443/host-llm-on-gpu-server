# Copy to .env and fill in. `.env` is gitignored — never commit real keys.
# Loaded automatically by every script (via python-dotenv) — no need to
# source or export this yourself.
# Get your key from https://cloud.vast.ai/account/
VAST_API_KEY=your-vast-ai-api-key-here
